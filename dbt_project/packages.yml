# dbt packages
packages:
  # dbt utilities for common macros (surrogate keys, date spine, etc.)
  - package: dbt-labs/dbt_utils
    version: ">=1.3.0"

  # Code generation utilities
  - package: dbt-labs/codegen
    version: ">=0.12.0"

  # Great Expectations-style data quality testing
  # Provides 50+ test types for comprehensive data validation
  - package: calogica/dbt_expectations
    version: ">=0.10.0"
